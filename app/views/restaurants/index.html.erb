<% @restaurants.each do |resto| %>
  <%= render "restaurant_card", restaurant: resto %>
<% end %>

<div class="col-xs-12 text-center">
  <%= link_to "Add a restaurant", new_restaurant_path, class: "btn btn-success" %>
</div>
